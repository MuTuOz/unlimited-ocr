# --- OCR model stack (transformers MUST stay on 4.x) ---------------------
# baidu/Unlimited-OCR remote code imports is_torch_fx_available and
# find_pruneable_heads_and_indices, both removed in transformers 5.x.
transformers==4.46.3
tokenizers==0.20.3
huggingface_hub==0.26.2
einops==0.8.0
addict==2.4.0
easydict==1.13

# --- document / image handling -------------------------------------------
pymupdf==1.24.13
pillow==11.0.0

# --- embeddings (ONNX, does NOT touch transformers) ----------------------
# Deliberately NOT sentence-transformers: it forces transformers 5.x and
# breaks the OCR model. fastembed runs the same BGE weights via onnxruntime.
fastembed==0.4.2

# --- vector store --------------------------------------------------------
# CPU build on purpose: the index is a few thousand vectors, and faiss-gpu
# would fight the OCR model for VRAM.
faiss-cpu==1.9.0

# --- LangChain (0.3.x keeps `from langchain.chains import RetrievalQA`) ---
langchain==0.3.7
langchain-core==0.3.15
langchain-community==0.3.5
langchain-text-splitters==0.3.2
langchain-groq==0.2.1

# --- misc ----------------------------------------------------------------
psutil==6.1.0
tqdm==4.67.0
